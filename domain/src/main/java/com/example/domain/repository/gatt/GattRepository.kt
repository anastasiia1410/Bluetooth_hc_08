package com.example.domain.repository.gatt

import kotlinx.coroutines.flow.MutableSharedFlow

interface GattRepository {
   // val dataFlow : MutableSharedFlow<ByteArray>
   // fun onSerialRead(data: ByteArray)
}